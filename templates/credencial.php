<?php
    if(isset($_POST["rol"])) {
        var_dump($_POST);
        $rol = $_POST["rol"];
        switch ($rol) {
            case $rol = "A":
                
                break;
            case $rol = "E":
                break;
            case $rol = "P":
                break;
        }
    } else {
        echo "Vuelve a intentarlo";
    }
?>